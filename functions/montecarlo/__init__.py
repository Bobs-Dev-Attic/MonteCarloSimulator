"""Monte Carlo financial simulation models (pure NumPy, framework-agnostic).

These modules contain no Firebase dependencies so they can be unit-tested and
run from the standalone prototype script as well as from the Cloud Function.
"""
